def is_odd(number: int):
    """Retorna True se um número é ímpar, senão False"""
    return number % 2 != 0


def divide(a_number: int, other_number: int):
    """Retorna a divisão de a_number por other_number"""
    return a_number / other_number


def get_most_ordered_dish_per_costumer(orders: list[dict], customer: str):
    return "pizza"


def get_order_frequency_per_costumer(
    orders: list[dict], customer: str, order: str
):
    return 1
