for id, count in birds.items():
    #     if count > count_ref:
    #         id_ref = id
    #         count_ref = count
    #     elif count == count_ref and id < id_ref:
    #         id_ref = id
    #         count_ref = count