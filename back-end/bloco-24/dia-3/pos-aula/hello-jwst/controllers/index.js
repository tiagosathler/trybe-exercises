const ping = require('./ping');
const login = require('./login');
const getUserMe = require('./getUserMe');
const getTopSecret = require('./getTopSecret');

module.exports = {
  ping,
  login,
  getUserMe,
  getTopSecret,
};
